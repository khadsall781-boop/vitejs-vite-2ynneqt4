import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.stormchaser.app',
  appName: 'Storm Chaser Tracker',
  webDir: 'dist'
};

export default config;
